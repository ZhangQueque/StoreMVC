function Money() {
    layer.open({
        type: 1,
        title: false,
        closeBtn: 0, //关闭按钮样式
        area: ['auto'],
        skin: 'layui-layer-nobg', //是否有背景色
        shadeClose: true, //是否点击遮罩关闭
        content: $('#Money')
    });
};

function QQ() {
    layer.open({
        type: 1,
        title: false,
        closeBtn: 0, //关闭按钮样式
        area: ['auto'],
        skin: 'layui-layer-nobg', //是否有背景色
        shadeClose: true, //是否点击遮罩关闭
        content: $('#QQ')
    });
};

function JueJin() {
    layer.open({
        type: 1,
        title: false,
        closeBtn: 0, //关闭按钮样式
        area: ['auto'],
        skin: 'layui-layer-nobg', //是否有背景色
        shadeClose: true, //是否点击遮罩关闭
        content: $('#JueJin')
    });
};

function Qun() {
    layer.open({
        type: 1,
        title: false,
        closeBtn: 0, //关闭按钮样式
        area: ['auto'],
        skin: 'layui-layer-nobg', //是否有背景色
        shadeClose: true, //是否点击遮罩关闭
        content: $('#Qun')
    });
};